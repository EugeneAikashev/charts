{{- if .Values.worker.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.name }}-worker
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.name }}
    role: worker
spec:
  replicas: {{ .Values.worker.replicaCount | default 2 }}
  progressDeadlineSeconds: 600
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: {{ .Values.name }}
      role: worker
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
      labels:
        app: {{ .Values.name }}
        role: worker
    spec:
      containers:
      - name: app
        env:
        - name: HOST_IP
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: status.hostIP
        - name: POD_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.name
        - name: NAMESPACE_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.namespace
        - name: CONSUL_HTTP_ADDR
          value: $(HOST_IP):8500
        - name: SERVICE_ROLE
          value: worker
        - name: SERVICE_NAME
          value: {{ .Values.env }}-{{ .Values.namespace }}-{{ .Values.name }}-workers
        {{- with .Values.worker.env }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        envFrom:
        - configMapRef:
            name: family.{{ .Values.namespace }}
        - configMapRef:
            name: app.{{ .Values.name }}
        - secretRef:
            name: app.{{ .Values.name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        livenessProbe:
          exec:
            command:
            - /bin/bash
            - -c
            - '! (supervisorctl status all | grep -qs FATAL)'
          failureThreshold: {{ .Values.worker.livenessProbe.failureThreshold | default 3 }}
          initialDelaySeconds: {{ .Values.worker.livenessProbe.initialDelaySeconds | default 10 }}
          periodSeconds: {{ .Values.worker.livenessProbe.periodSeconds | default 10 }}
          successThreshold: {{ .Values.worker.livenessProbe.successThreshold | default 1 }}
          timeoutSeconds: {{ .Values.worker.livenessProbe.timeoutSeconds | default 3 }}
        resources:
          limits:
            cpu: {{ .Values.worker.resources.limits_cpu | default "100m" }}
            memory: {{ .Values.worker.resources.limits_memory | default "128Mi" }}
          requests:
            cpu: {{ .Values.worker.resources.requests_cpu | default "200m" }}
            memory: {{ .Values.worker.resources.requests_memory | default "256Mi" }}
        volumeMounts:
        - mountPath: /app/storage/logs/json
          name: logs-app
          subPathExpr: $(NAMESPACE_NAME)_$(POD_NAME)
      imagePullSecrets:
      - name: docker-auth
      terminationGracePeriodSeconds: {{ .Values.worker.terminationGracePeriodSeconds | default 30 }}
      volumes:
      - hostPath:
          path: {{ .Values.logs.app }}
          type: ""
        name: logs-app
{{- end -}}
